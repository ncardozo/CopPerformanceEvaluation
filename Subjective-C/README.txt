To execute each file compile using the command line as:
gcc -framework Foundation -o #{name_of_file} #{name_of_file}.m

then execute each file by just run the executable generated.


All results are saved in the root documents folder, respectively in
runs_ifs
runs_strategy
runs_contexts